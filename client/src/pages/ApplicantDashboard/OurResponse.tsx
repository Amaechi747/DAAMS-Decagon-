import * as React from 'react';
import { ApplicantsSidebar } from "../../components"

interface IOurResponseProps {
}

const OurResponse: React.FunctionComponent<IOurResponseProps> = (props) => {
 return (
  <>
   <ApplicantsSidebar />
  </>
 );
};

export default OurResponse;
